{
  "name": "shopipoints.com",
  "version": "1.0.0",
  "description": "Moar Shopipoints",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "deploy": "node scripts/deploy.js",
    "deploy:production": "node scripts/deploy.js -- --env=production --allowLive=true",
    "watch": "node scripts/watch.js",
    "lint:js": "eslint ./**/*.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/t-kelly/shopipoints.com.git"
  },
  "keywords": [
    "Shopify",
    "KellyVaughn"
  ],
  "author": "The Kellys",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/t-kelly/shopipoints.com/issues"
  },
  "homepage": "https://github.com/t-kelly/shopipoints.com#readme",
  "devDependencies": {
    "@babel/core": "^7.12.3",
    "@babel/eslint-parser": "^7.12.1",
    "@babel/plugin-syntax-top-level-await": "^7.12.1",
    "@shopify/eslint-plugin": "^39.0.3",
    "@shopify/themekit": "^1.1.6",
    "eslint": "^7.13.0",
    "yargs": "^16.1.1"
  },

  "@comment eslintConfig": "By default we use Shopify's esnext rules to style our JavaScript. We need the following override to handle the Node v14+ syntax that we use in our NPM scripts, e.g. Top Level Awaits",
  "eslintConfig": {
    "extends": ["plugin:@shopify/esnext"],
    
    "overrides": [
      {
        "extends": ["plugin:@shopify/node"],
        "files": ["./scripts/*.js"],
        "parser": "@babel/eslint-parser",
        "parserOptions": {
          "ecmaVersion": 2021,
          "sourceType": "module"
        }
      }
    ]
  },

  "@comment babel": "We only need babel because ESLint currently does not support TopLevelAwait",
  "babel": {
    "plugins": ["@babel/plugin-syntax-top-level-await"]
  }
}
